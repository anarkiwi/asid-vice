---
name: test build
on: [push, pull_request]
jobs:
  test-build-latest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive
      - name: test build
        run: ./build.sh
