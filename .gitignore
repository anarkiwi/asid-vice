autom4te.cache/
config.log
config.status
Doxyfile
Makefile
*.o
*.a
.deps/

# Generated by byacc/flex from src/monitor/mon_parse.y and mon_lex.l at build
# time. These were previously committed and went stale (missing the keymatrix /
# screenscrape tokens), which made fresh builds non-deterministically fail with
# "'CMD_KEYMATRIX' undeclared" depending on checkout mtimes. Never commit them.
/src/monitor/mon_parse.c
/src/monitor/mon_parse.h
/src/monitor/mon_lex.c
